import 'package:flutter/widgets.dart';

class MyPack extends StatelessWidget {
  const MyPack({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset("assets/1.jpg");
  }
}
